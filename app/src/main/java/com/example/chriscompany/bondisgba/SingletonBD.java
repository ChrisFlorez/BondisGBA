package com.example.chriscompany.bondisgba;

/**
 * Created by chris on 24/01/17.
 */
public class SingletonBD {

      /*  private BaseDeDatos db;
        private static SingletonBD soyUnicoBD;

        // El constructor es privado, no permite que se genere un constructor por defecto.
        private SingletonBD(BaseDeDatos db) {
            this.db = db;
            System.out.println("se creo correctamente la db");
        }

        public static SingletonBD getSingletonInstance(BaseDeDatos db) {
            if (soyUnicoBD == null){
                soyUnicoBD = new SingletonBD(db);
            }
            else{
                System.out.println("No se puede crear la base porque ya existe un objeto de la clase");
            }

            return soyUnicoBD;
        }
        public BaseDeDatos getBaseDeDatos(){
            return this.db;
        }
        // metodos getter y setter

    }*/
}
